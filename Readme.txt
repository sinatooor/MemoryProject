The main python code, kivy design file and database files are inside the source code folder.
In order to run the source code you need to install kivy package first. Otherwise you can run the exe file of application in /dist folder without installing anything.
